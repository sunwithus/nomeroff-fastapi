fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6
rapidocr-onnxruntime>=1.3.0
# RapidOCR (двухстрочные номера + OSD) по умолчанию тянет CPU-сборку onnxruntime,
# из-за чего весь этот путь идёт на процессоре даже при живой CUDA.
# Ставить только на машинах с NVIDIA; на CPU-машине строку закомментировать.
onnxruntime-gpu>=1.17.0
# easyocr>=1.7.0  # optional fallback for ocr_overlay
